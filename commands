How to create cluster 

eksctl create cluster --name shareef786 --nodegroup-name shaik --node-type m7i-flex.large --nodes 2 --managed



curl -sSL -o eksctl.tar.gz https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_${PLATFORM}.tar.gz

tar -xzf eksctl.tar.gz

mv eksctl /usr/local/bin/

eksctl version



curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"

chmod +x kubectl

mv kubectl /usr/local/bin/

kubectl version --client

